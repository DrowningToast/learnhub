{
    "private": true,
    "type": "module",
    "scripts": {
        "dev": "vite & php artisan serve --port=3000",
        "build": "vite build",
        "compose": "docker-compose -f docker-compose.yml up -d --build",
        "setup": "composer install && npm install -g pnpm && pnpm install && pnpm run compose"
    },
    "devDependencies": {
        "autoprefixer": "^10.4.18",
        "axios": "^1.6.4",
        "laravel-vite-plugin": "^1.0.0",
        "postcss": "^8.4.35",
        "tailwindcss": "^3.4.1",
        "vite": "^5.0.0"
    }
}